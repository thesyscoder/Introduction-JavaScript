// conditions in js

const age = 19;

const isOld = age >= 18;

console.log(isOld);

if (age >= 18) {
  console.log(`You are now ${age} !!!`);
}

// if else in javascript

const color = "red";

if (color === "red") {
  console.log("Awesome Red Color");
} else {
  console.log(`Entered color ${color}`);
}
